{
  "scripts": {
    "test": "cypress run",
    "test:open": "cypress open",
    "test:chrome": "cypress run --browser chrome",
    "test:report": "cypress run && npx mochawesome-merge evidencias/report/*.json -o evidencias/report/merged.json && npx marge evidencias/report/merged.json -o evidencias/report"
  },
  "devDependencies": {
    "axe-core": "^4.11.1",
    "cypress": "^15.11.0",
    "cypress-axe": "^1.7.0",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.1.1",
    "mochawesome-report-generator": "^6.3.2"
  }
}
